# Kameleoon A/B Test Conversion Chart

#### Интерактивная линейная диаграмма для визуализации коэффициента конверсии вариантов A/B-теста на основе данных из data.json.

#### Проект написан на React 18 + TypeScript и собран с помощью Vite.

# 🚀 Демо

- Доступно по [ссылке](https://tema-skakun.github.io/kameleoon/)

# 🛠️ Стек

- React 18

- TypeScript

- Vite

- Recharts - библиотека для визуализации данных

- html2canvas - экспорт диаграммы в PNG

- CSS Modules - модульные стили

- Без состояния на сервере, всё работает целиком в браузере

# 📈 Выбранная библиотека визуализации

Для построения диаграммы используется [Recharts](https://recharts.org/en-US/):

- декларативный API, хорошо подходит для React;

- удобная работа с tooltip, легендой, сеткой;

- поддержка адаптивной вёрстки через ResponsiveContainer;

- возможность кастомизации tooltip и курсора для реализации вертикальной линии и всплывающего окна.

# 🎯 Реализованные требования

## Обязательные

- Линейный график коэффициента конверсии (conversion rate) для всех вариантов.

- Расчёт конверсии по формуле:

``
conversionRate = conversions/visits×100
``

- Все значения на оси Y и в tooltip отображаются в процентах.

    - При наведении курсора:

        - отображается вертикальная линия (cursor Recharts);

        - показывается кастомный tooltip с ежедневными данными по всем выбранным вариантам:

            - дата / период;

            - коэффициент конверсии;

            - посещения;

            - конверсии.

- Минимум один вариант всегда выбран - при попытке снять последний чекбокс он остаётся активным.

- Ось Y автоматически адаптируется под:

    - текущий видимый диапазон точек;

    - текущий набор выбранных вариантов (минимум и максимум считаются по видимым сериями).

- Ось X адаптируется к видимому диапазону данных через собственный «brush»-диапазон:

    - данные диаграммы нарезаются по индексу startIndex / endIndex;

    - при изменении интервала видно только соответствующий участок по оси X.

- Адаптивный макет:

    - ширина контейнера диаграммы ограничена от 671px до 1300px;

    - сама диаграмма резиновая за счёт ResponsiveContainer.

- Элементы управления:

    - Выбор вариантов (флажки для каждого варианта A/B-теста);

    - Выбор режима "День / Неделя":

        - день - исходные данные по датам;

        - неделя - агрегированные блоки по 7 последовательных дней.

## Бонусные функции (реализовано)

- Масштабирование / панорамирование / сброс масштаба

- Кастомный хук useBrush:

    - zoomIn / zoomOut (+/- по количеству видимых точек);

    - panLeft / panRight (прокрутка окна по оси X);

    - reset - возврат к полному диапазону данных.

- Выбор стиля линий

    - Линия - обычная линейная диаграмма;

    - Сглаживание - тип линии monotone в Recharts;

    - Область - area chart с полупрозрачным заполнением.

- Переключение светлой/тёмной темы

    - Используются CSS-переменные:

        - светлая тема по умолчанию;

        - тёмная тема включает другой набор переменных (.appDark).

- Экспорт диаграммы в PNG

    - Кнопка «PNG» делает скриншот контейнера диаграммы с помощью html2canvas;

    - результат скачивается как ab-test-conversion-chart.png.

## Данные и расчёт метрики

Источник данных: src/data/data.json

### Структура:

- variations - список вариантов:

    - name - отображаемое имя варианта;

    - id - числовой идентификатор варианта (для Original в исходных данных используется ключ "0").

- data - массив точек:

    - date - дата в формате YYYY-MM-DD;

    - visits - объект, где ключ - строковый ID варианта ("0", "10001", ...), значение - количество посещений;

    - conversions - объект с конверсиями по тем же ключам.

### Парсинг и расчёт:

- В parseRawData данные преобразуются в массив ParsedDataPoint:

    - дата как Date;

    - conversionRate по каждому варианту:

``
conversionRate[key] = (conv / visits) * 100;
``

- #### если посещений нет или visits === 0, значение конверсии - null.

- В недельном режиме (aggregateWeekly) данные группируются по блокам по 7 последовательных дней,
  суммы visits и conversions агрегируются, и уже по этим суммам пересчитывается conversionRate.

# 🏗️ Структура проекта

Основные файлы:

- index.html - точка входа для Vite.

- src/main.tsx - монтирование React-приложения.

- src/App.tsx - корневой компонент:

    - загрузка data.json;

    - управление режимами (день/неделя, стиль линий, тема);

    - работа с hook useBrush (зум/панорамирование);

    - экспорт в PNG.

- src/App.module.css - глобальные стили и CSS-переменные тем.

- src/components/ChartControls/ChartControls.tsx

- Панель управления:

    - выбор вариантов;

    - переключатель «День / Неделя»;

    - выбор стиля линий;

    - переключатель темы;

    - кнопки зума / панорамирования / сброса;

    - экспорт в PNG.

- src/components/ChartControls/ChartControls.module.css - стили панели управления.

- src/components/ConversionChart/ConversionChart.tsx
- Компонент диаграммы:

    - построение LineChart / AreaChart (Recharts);

    - кастомный tooltip;

    - динамический yDomain по видимым значениям и выбранным вариантам.

- src/components/ConversionChart/ConversionChart.module.css - стили диаграммы и tooltip.

- src/hooks/useBrush.ts - хук для управления видимым интервалом (зум/панорамирование).

- src/utils/transformData.ts - парсинг, агрегация и подготовка данных к визуализации.

- src/types.ts - типы для данных и конфигураций.

- src/data/data.json - тестовые данные A/B-теста.

- vite.config.ts - конфигурация Vite, base: '/kameleoon/' для корректной работы на GitHub Pages.

- tsconfig.json - конфигурация TypeScript (строгий режим).

# 📦 Установка и запуск

### Установка

```bash
# Клонирование репозитория
git clone git@github.com:tema-skakun/kameleoon.git
cd kameleoon

# Установка зависимостей
npm install
```

### Разработка

```bash
# Запуск в режиме разработки
npm run dev
```

Приложение будет доступно по адресу http://localhost:5173

### Сборка

```bash
# Сборка для production
npm run build

# Превью сборки
npm run preview
```

### Деплой

```bash
# Деплой на GitHub Pages
npm run deploy
```
